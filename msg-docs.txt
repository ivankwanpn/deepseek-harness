docs: bring the generated references' Chinese pairs back in sync

Three generated references were still stale after the goal-budget change, and the
two catalogs regenerated in the previous commit left their Chinese counterparts
behind. All of them are bilingual pairs, so each needs both sides plus a
re-recorded pairing entry:

- docs/event-producer-consumer.md — the goal event rows still cited the source
  line numbers from before the change.
- docs/persistence-catalog.md — the `goal/change` source reference likewise.
- docs/config-catalog.md / docs/tool-catalog.md — the English sides landed
  already; their `.zh.md` counterparts now carry the same new entries.

Every one of these gates exits 0 afterwards: verify-doc-graphs,
verify-persistence-catalog, verify-config-catalog, verify-tool-catalog,
verify-cordis-catalog, and verify-translation-pairing. All six passed at
86b3f85818 and failed before this commit, so the whole set is a regression from
the budget change rather than pre-existing drift.

`pnpm run doc-sync` is green: 34 passed, 0 failed. `pnpm run test:docs` is green:
16 passed, 0 failed.

This is the third and last class of generated output the budget change made
stale, after the snapshot sidecars and the catalogs themselves. All of it is
freshness-gated, and none of it is covered by the quick doc aggregate that was
run with the original change.
