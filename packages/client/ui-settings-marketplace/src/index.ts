/** Host loader entry for the marketplace-tab browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the marketplace settings tab. */
export function apply(): void {}
